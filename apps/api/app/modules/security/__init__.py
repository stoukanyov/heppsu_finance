"""Сигурност — контроли, които не принадлежат на конкретен бизнес контекст.

Тук живее това, което пази *достъпа* до системата (ограничаване на опитите за
вход), а не това, което пази конкретни данни — правата върху компания са в
`modules.rbac`, одитната следа е в `modules.audit`.
"""
